# BooksCollection

This repository contains a collection of TXT books organized for use with library applications.

## Structure

- Each book is stored as a `.txt` file.
- Chapters within a book are marked using `##`.
- Pages within a chapter are separated using `PAGE_SEPARATOR`.
- Some books may be organized inside subfolders.

## Usage

- You can clone or download this repository to your local machine:
  ```bash
  git clone https://github.com/Ciyoku/BooksCollectiony.git
